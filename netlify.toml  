[build]
  command = "pnpm build"
  publish = ".next"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["netlify/functions/emails/**"]

# Scheduled job every 5 minutes
[[scheduled]]
  name = "send-newsletters-batch"
  cron = "*/5 * * * *"
